# Hardware control
RPi.GPIO
Adafruit_DHT
gpiozero
adafruit-circuitpython-ads1x15

# GUI
tk   # tkinter is usually built-in, so no need to install via pip

# Optional networking (if needed later)
requests
paho-mqtt